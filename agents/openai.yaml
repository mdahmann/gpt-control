interface:
  display_name: "GPT-Control Pro Worker"
  short_description: "Orchestrate exact, durable ChatGPT Pro web workers"
  default_prompt: "Use GPT-Control only for a bounded independent review or task. Keep Codex as the orchestrator, require an idempotency key, request connectors explicitly when relevant, and consume one completion or blocker result instead of polling."
policy:
  allow_implicit_invocation: true
